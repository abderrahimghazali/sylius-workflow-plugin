{% set status_value = data.value is defined ? data.value : data %}
{% set colors = {
    'draft': 'secondary',
    'active': 'success',
    'paused': 'warning',
} %}
<span class="badge text-white bg-{{ colors[status_value] ?? 'secondary' }}">
    {{ ('workflow.ui.' ~ status_value)|trans }}
</span>
